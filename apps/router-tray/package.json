{
  "name": "@codex-failover/router-tray",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/popup.html src/popup.css src/popup-logic.js src/popup-render.js dist/ && cp assets/*.png dist/",
    "dev": "tsc -p tsconfig.json --watch & cp src/popup.html dist/ && cp assets/*.png dist/",
    "start": "electron dist/main.js",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.7",
    "electron": "^33.0.0",
    "typescript": "^5.7.3"
  }
}
