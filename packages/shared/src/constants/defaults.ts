export const DEFAULTS = {
  PORT: 8787,
  BIND_ADDRESS: '127.0.0.1',
  PRIMARY_COOLDOWN_TTL_MS: 6 * 60 * 60 * 1000,
  AZURE_COOLDOWN_TTL_MS: 1 * 60 * 60 * 1000,
  PROVIDER_TIMEOUT_MS: 30000,
  REMAINING_RATIO_THRESHOLD: 0.03,
  STREAM_FIRST_BYTE_TIMEOUT_MS: 5000,
  DATA_DIR_ENV: 'CODEX_FAILOVER_DATA_DIR',
  USER_DATA_DIR_NAME: '.codex-failover',
} as const;
